
<% if alert.present? || flash[:alert].present? %>
  <p class="alert alert-danger"><%= alert %></p>
<% elsif notice.present? || flash[:notice].present? %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>

